# my-site

Personal site hosted via GitHub Pages.

## Local preview

```bash
python3 -m http.server 8000
```

Then open http://localhost:8000.

## Deploy

Pushes to `main` are published automatically by GitHub Pages once enabled in repo settings.
